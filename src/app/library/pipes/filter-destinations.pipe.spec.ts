import { FilterDestinationsPipe } from './filter-destinations.pipe';

describe('FilterDestinationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDestinationsPipe();
    expect(pipe).toBeTruthy();
  });
});
